import { Link } from "react-router-dom";
import { useState } from "react";

// Styles
import styles from "./Header.module.css";

export default function Header({ isTokenPresent }) {
  return (
    <header>
      <div>Strateg In</div>
    </header>
  );
}
